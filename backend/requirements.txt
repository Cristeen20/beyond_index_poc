# Local dev manifest. The deployed function installs from the root
# requirements.txt instead — keep the two in sync when adding a dependency.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
openai>=1.35.0
pydantic>=2.9.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
twilio>=9.3.0
langgraph>=0.2.60

# Durable checkpointer. Required on serverless (Vercel), where the
# in-process MemorySaver cannot survive across invocations. graph/session.py
# falls back to MemorySaver when DATABASE_URL is unset, so these are inert
# for a purely local run.
langgraph-checkpoint-postgres>=3.1.2
psycopg[binary,pool]>=3.2.0
